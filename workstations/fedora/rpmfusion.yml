- name: Enable RPM Fusion free repository
  ansible.builtin.dnf:
    name: "https://download1.rpmfusion.org/free/fedora/rpmfusion-free-release-{{ ansible_distribution_major_version }}.noarch.rpm"
    state: present
    disable_gpg_check: true
  register: fedora_rpmfusion_free_repository

- name: Enable RPM Fusion nonfree repository
  ansible.builtin.dnf:
    name: "https://download1.rpmfusion.org/nonfree/fedora/rpmfusion-nonfree-release-{{ ansible_distribution_major_version }}.noarch.rpm"
    state: present
    disable_gpg_check: true
  register: fedora_rpmfusion_nonfree_repository

- name: Refresh DNF cache
  ansible.builtin.dnf:
    update_cache: true
  when: >
    not ansible_check_mode
    or not (fedora_rpmfusion_free_repository.changed or fedora_rpmfusion_nonfree_repository.changed)
